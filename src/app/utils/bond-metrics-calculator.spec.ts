import { BondMetricsCalculator } from './bond-metrics-calculator';

describe('BondMetricsCalculator', () => {
  it('should create an instance', () => {
    expect(new BondMetricsCalculator()).toBeTruthy();
  });
});
