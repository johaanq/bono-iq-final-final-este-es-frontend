import { FinanlcialMetricModel } from './finanlcial-metric.model';

describe('FinanlcialMetricModel', () => {
  it('should create an instance', () => {
    expect(new FinanlcialMetricModel()).toBeTruthy();
  });
});
