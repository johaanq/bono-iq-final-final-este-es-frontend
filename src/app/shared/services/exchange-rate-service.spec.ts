import { ExchangeRateService } from './exchange-rate-service';

describe('ExchangeRateService', () => {
  it('should create an instance', () => {
    expect(new ExchangeRateService()).toBeTruthy();
  });
});
